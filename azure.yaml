name: azd-foundry-multi-agent
metadata:
  template: azd-starter-bicep

hooks:
  postprovision:
    # - shell: pwsh
    #   run: scripts/capability-host.ps1
    - shell: pwsh
      run: scripts/deploy-webapp.ps1
    # - shell: pwsh
    #   run: scripts/load-data.ps1
